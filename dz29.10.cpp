#include <iostream>;
#include "math.h"
using namespace std;
int main()
{
    setlocale(LC_ALL, "RUS");
    double x, y;
    cout << "введите х: ";
    cin >> x;
    cout << "введите y: ";
    cin >> y;

    if ((y <= 0, 5 * x + 1) && (y >= -0, 5 * x - 1) or ((1 >= pow(y, 2) + pow(x, 2)) and x >= 0))
    {cout << "входит ";}
    else { cout << "не входит "; }
